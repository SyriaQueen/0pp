// Keep the buildscript block (this is for Gradle plugins, not app dependencies)
buildscript {
    repositories {
        google()
        mavenCentral()
    }
    dependencies {
        classpath 'com.android.tools.build:gradle:8.2.0'
    }
}

// REMOVE THIS ENTIRE BLOCK:
// allprojects {
//    repositories {
//        google()
//        mavenCentral()
//    }
// }

task clean(type: Delete) {
    delete rootProject.buildDir
}
